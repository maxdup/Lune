<UserControl x:Class="Lune.Views.v_LibrarySubsetting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="150">
    <StackPanel>
        <Label Content="Folders to scan" FontSize="18" Foreground="LightGray"/>
        <ListBox ItemsSource="{Binding paths}" 
                 Style="{StaticResource LibraryListBoxStyle}" 
                 ItemContainerStyle="{StaticResource SettingsListBoxItemStyle}">

        </ListBox>
        <Button Content="Add" Command="{Binding settingsCtrl}" CommandParameter="AddDir" Margin="0 10 5 0"/>
    </StackPanel>
</UserControl>
