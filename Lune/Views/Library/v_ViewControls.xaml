<UserControl x:Class="Lune.Views.v_ViewControls"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
        <Button x:Name="vArtist"  Style="{StaticResource viewControl}" Content="Artists" Command="{Binding libViews}"  CommandParameter="Artists"/>
        <Button x:Name="vAlbum"  Style="{StaticResource viewControl}" Content="Albums" Command="{Binding libViews}"  CommandParameter="Albums"/>
        <Button x:Name="vSong" Style="{StaticResource viewControl}" Content="Songs" Command="{Binding libViews}"  CommandParameter="Songs"/>
    </StackPanel>
</UserControl>
