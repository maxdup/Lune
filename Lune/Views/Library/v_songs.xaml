<UserControl x:Class="Lune.Views.v_songs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid HorizontalAlignment="Stretch">
        <ListBox x:Name="listB_Songs" ItemsSource="{Binding songsDisplayed}" ListBoxItem.MouseDoubleClick="sendListForPlay" Style="{StaticResource LibraryListBoxStyle}" ItemContainerStyle="{StaticResource LibraryListBoxItemStyle}"/>
        <!--<ListBox x:Name="listB_Songs" ItemsSource="{Binding songsDisplayed}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <EventSetter Event="MouseDoubleClick" Handler="sendListForPlay"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>-->
    </Grid>
</UserControl>
