<UserControl x:Class="Lune.Views.v_playbackStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel LastChildFill="True">
        <Label Content="{Binding currSongInfo}" DockPanel.Dock="Top" Foreground="#CCCCCC" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        <DockPanel DockPanel.Dock="Bottom">
            <Label Content="{Binding currentTime}" DockPanel.Dock="Left" Foreground="#CCCCCC"/>
            <Label Content="{Binding songLength}" DockPanel.Dock="Right" Foreground="#CCCCCC" HorizontalAlignment="Right"/>
        </DockPanel>
        <Slider Style="{StaticResource songProgress}" HorizontalAlignment="Stretch" IsMoveToPointEnabled="True" Value="{Binding SliderPosition, Mode=TwoWay}" />
    </DockPanel>
</UserControl>
