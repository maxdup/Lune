<UserControl x:Class="Lune.Views.v_playbackStatus"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <Label Content="{Binding currSongInfo}" Foreground="#CCCCCC" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        <Slider Style="{StaticResource songProgress}" Width="250" HorizontalAlignment="Center" Value="{Binding SliderPosition, Mode=TwoWay}" />
        <DockPanel>
            <Label Content="{Binding currentTime}" DockPanel.Dock="Left" Foreground="#CCCCCC"/>
            <Label Content="{Binding songLength}" DockPanel.Dock="Right" Foreground="#CCCCCC" HorizontalAlignment="Right"/>
        </DockPanel>
    </StackPanel>
</UserControl>
